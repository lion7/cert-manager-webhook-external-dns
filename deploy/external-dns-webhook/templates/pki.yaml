---
# Create a selfsigned Issuer, in order to create a root CA certificate for
# signing webhook serving certificates
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "external-dns-webhook.selfSignedIssuer" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "external-dns-webhook.labels" . | nindent 4 }}
spec:
  selfSigned: {}

---

# Generate a CA Certificate used to sign certificates for the webhook
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "external-dns-webhook.rootCACertificate" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "external-dns-webhook.labels" . | nindent 4 }}
spec:
  secretName: {{ include "external-dns-webhook.rootCACertificate" . }}
  duration: 43800h # 5y
  issuerRef:
    name: {{ include "external-dns-webhook.selfSignedIssuer" . }}
  commonName: "ca.external-dns-webhook.cert-manager"
  isCA: true

---

# Create an Issuer that uses the above generated CA certificate to issue certs
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "external-dns-webhook.rootCAIssuer" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "external-dns-webhook.labels" . | nindent 4 }}
spec:
  ca:
    secretName: {{ include "external-dns-webhook.rootCACertificate" . }}

---

# Finally, generate a serving certificate for the webhook to use
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "external-dns-webhook.servingCertificate" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "external-dns-webhook.labels" . | nindent 4 }}
spec:
  secretName: {{ include "external-dns-webhook.servingCertificate" . }}
  duration: 8760h # 1y
  issuerRef:
    name: {{ include "external-dns-webhook.rootCAIssuer" . }}
  dnsNames:
  - {{ include "external-dns-webhook.fullname" . }}
  - {{ include "external-dns-webhook.fullname" . }}.{{ .Release.Namespace }}
  - {{ include "external-dns-webhook.fullname" . }}.{{ .Release.Namespace }}.svc
