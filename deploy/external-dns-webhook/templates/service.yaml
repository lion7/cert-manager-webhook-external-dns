apiVersion: v1
kind: Service
metadata:
  name: {{ include "external-dns-webhook.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "external-dns-webhook.name" . }}
    chart: {{ include "external-dns-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "external-dns-webhook.name" . }}
    release: {{ .Release.Name }}
